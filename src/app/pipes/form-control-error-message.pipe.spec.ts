import { FormControlErrorMessagePipe } from './form-control-error-message.pipe';

describe('FormControlErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
