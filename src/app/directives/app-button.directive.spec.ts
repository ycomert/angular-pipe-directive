import { AppButtonDirective } from './app-button.directive';

describe('AppButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AppButtonDirective();
    expect(directive).toBeTruthy();
  });
});
