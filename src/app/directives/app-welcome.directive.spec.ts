import { AppWelcomeDirective } from './app-welcome.directive';

describe('AppWelcomeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppWelcomeDirective();
    expect(directive).toBeTruthy();
  });
});
