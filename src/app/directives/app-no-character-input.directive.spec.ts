import { AppNoCharacterInputDirective } from './app-no-character-input.directive';

describe('AppNoCharacterInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNoCharacterInputDirective();
    expect(directive).toBeTruthy();
  });
});
