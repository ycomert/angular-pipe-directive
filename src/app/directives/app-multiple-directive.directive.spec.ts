import { AppMultipleDirectiveDirective } from './app-multiple-directive.directive';

describe('AppMultipleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMultipleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
